# 架設環境
參考來源: 黃仕翔學長整理https://github.com/frogg3b0/NVIDIA-Sionna/blob/main/Sionna%20%E5%89%8D%E7%BD%AE%E4%BD%9C%E6%A5%AD/%E6%9E%B6%E8%A8%AD%E7%92%B0%E5%A2%83.md
## 1. 安裝流程
### 1-1. 使用設備
我們這次使用的設備一共有二:  
1. 本地電腦 (Windows)
2. 遠端server (Ubuntu 24.04)

因此我們會透過在前端使用 Windows 系統，並且在 `cmd` 內遠端操作。即在 Ubuntu 上架設一個 Python virtual environment。



### 1.2 安裝流程 (無腦照做即可)

0. 在 windows 上使用 `ssh` 遠端連接 server

```python
ssh <User name>@ <server ip>
```

1. 查詢 Python 版本
```python
python3 --version
```

2. 安裝 python3-venv
```python
sudo apt update
sudo apt install python3.12-venv  
```

3. 建立虛擬環境
```python
python3 -m venv sionna_env
```

4. 啟用虛擬環境
```python
source sionna_env/bin/activate
```
#### 此時你的提示字前面應該會變成這樣:`(sionna_env) wicoms5090@wicoms5090:~$`

5. 安裝 Sionna
```python
pip install --upgrade pip
pip install sionna
```

6. 安裝 jupyter lab
```python
pip install jupyterlab ipykernel
```

7. 註冊新環境到jupyter
```python
python -m ipykernel install --user --name=sionna_env --display-name "Python (sionna_env)"
```

8. 建一個 tmux session 讓 Jupyter 不會因為你斷線就關掉

```python
tmux new -s jl
```

## 2. 如何使用 Jupyter notebook (安裝完直接看這裡)
###情況一：Server 沒重啟，只是斷線重連
1. 開啟第一個cmd並遠端連接server
```python
ssh  <User name>@<server ip>
```

2. 若已有tmux，則加入現有的session
```python
tmux attach -t <自己設定user name>
```

3.開啟第二個cmd並遠端連接server
```python
ssh -L 8889:127.0.0.1:8889 <User name>@<server ip>
```

4. 啟用虛擬環境
```python
source ~/sionna_env/bin/activate
```

5. 開啟jupyter notebook
```python
jupyter lab --no-browser --ip=127.0.0.1 --port=8889
```

###情況二：Server 重啟過，什麼都沒了
1. 開啟第一個cmd並遠端連接server
```python
ssh  <User name>@<server ip>
```

2. 建一個 tmux session 讓 Jupyter 不會因為你斷線就關掉
```python
tmux new -s <自己設定user name>
```

3. 啟用虛擬環境
```python
source ~/sionna_env/bin/activate
```

4.開啟第二個cmd並遠端連接server
```python
ssh -L 8889:127.0.0.1:8889 <User name>@<server ip>
```

5. 啟用虛擬環境
```python
source ~/sionna_env/bin/activate
```

6. 開啟jupyter notebook
```python
jupyter lab --no-browser --ip=127.0.0.1 --port=8889
```

---
## 3. 重開機從 windows 轉成 Ubuntu
* 確認 windows 沒人使用後，須至 server 端重開電腦
* 按下重啟鍵後，按 `F2` 進入 BIOS
* 點選右側啟動選單的 「KINSTON」SSD ，進入Ubuntu24.04系統
  
## 4. 錯誤排查
* 如果Server重開機無法連線，高機率是 ip 被更換了
* 此時只需要在 server 端輸入`ip a`
  * 此時在終端機第二列 eno1 會顯示有線ip `192.168.50.xxx`
* 之後只需在本地電腦使用該 ip 即可連線 

### 若在 server 端輸入`ip a`沒有看到有線ip
1. 先重新開機
```python
sudo reboot
```

2. 開機時持續按住或連按 `Shift`

3. 進入 GRUB 後選 `Advanced options for Ubuntu`

4.1. 再選 `Ubuntu, with Linux 6.8.0-107-generic`, 不要選 recovery mode

4.2. 沒看到`Ubuntu, with Linux 6.8.0-107-generic`， 那就要重新安裝107版

4.2.1. 安裝107版
```python
sudo apt install \
linux-image-6.8.0-107-generic \
linux-modules-6.8.0-107-generic \
linux-modules-extra-6.8.0-107-generic \
linux-headers-6.8.0-107-generic
```

4.2.2. 安裝後重新做1.~4.

5. 等他跑完在 server 端重新輸入`ip a`應該就會跑出來了
